# Dependencies
node_modules/

# Claude Code internal files
skills-lock.json

# Supabase CLI temp files (local state — root and any nested app)
**/supabase/.temp/

# Local Netlify folder
.netlify

# Local environment variables — never commit secrets
.env
Background docs/2026 - Sergio Parisse Appreciation League (3).xlsx

# Vite build outputs — generated, not source code
/spal/
/fpl/

# Local env files — may contain real credentials
**/.env.local
**/.env

# Python virtualenvs
data-pipeline/**/.venv/

# Python bytecode
__pycache__/
*.pyc

# FPL raw API cache — may be large. The compressed raw-<season>.tar.gz archive
# is committed to the repo as the backup; the uncompressed cache stays ignored.
data-pipeline/fpl/raw/

# Temp extraction folder
BadgerFPLsetup/unzipped/

# macOS junk
.DS_Store

# Local-only files — ignored by specific path, NOT by extension
# (deliberately not *.zip / *.numbers, so future tracked files aren't swallowed)
Book1.numbers
BadgerFPLsetup.zip
